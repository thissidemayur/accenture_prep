## Dynamic ARP Inspection (DAI):
- validate address resoulation protocol(ARP) packet on network. 
- it intercepts ARP request and response, dropping any fake ones to prevent attackers from rerouting network traffic through their own device(ARP posigion,spoofing)

what is spoofing
## IP Source Guard(IPSG): 
- it prevent IP address spoogfing in layer2.  it inspect all ip traffic entering a switch port and drop any packet where the source Ip address or mac address doesnot match the switch's authorized DHCR database or static bindings

## Port Security
- it limits access to seitch port based on specific MAC address. it resetric how many device can connect to a single ethernet port and shuts down or drop traffic if an unthorized device plugin

## Access Control List(ACL): 
is a set of applied routers or firewalls that permit or denies traffic based on criteria such as source/destination IP, protocol types or port number.


## GRE Tunnel( genereaic routing encapsulation)
